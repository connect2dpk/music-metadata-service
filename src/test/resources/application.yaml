spring:
  application:
    name: music-metadata-service-test

  docker:
    compose:
      enabled: false

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

logging:
  level:
    org.flywaydb: WARN